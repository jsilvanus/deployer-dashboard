# Example build-time env vars for the dashboard (Vite inlines variables prefixed with VITE_)
# Copy to .env.local to override locally (do not commit .env.local)

VITE_DEFAULT_DEPLOYER_URL=https://example.com

